local deftest = require "deftest.deftest"

local test_color = require "main.test_color"
local test_math = require "main.test_math"

local harmony = require "color.harmony"
local color = require "color.color"

function test()
	local c = harmony.monochromatic(color.from_hex("#804080"))
	pprint(c)
end

function init(self)
	deftest.add(test_color)
	deftest.add(test_math)
	--deftest.run()
	test()
end
